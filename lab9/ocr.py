from numpy import fft


class Ocr:
    def __init__(self):
        self.chars_order = [chr(i) for i in range(ord('a'), ord('z'))]\
            + [chr(i) for i in range(ord('A'), ord('Z')]
            + 

    def load_alphabet(self, alphabet_path):
        pass
